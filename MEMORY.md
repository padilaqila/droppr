# MEMORY.md — Catatan Kesalahan & Pembelajaran Proyek Droppr

> File ini ditulis oleh agen AI, bukan oleh manusia. Dibaca otomatis di awal sesi (bagian atas file diprioritaskan). Lihat `AGENTS.md` §5 untuk format entri dan aturan pemangkasan.

## [2026-09-10] Eliminasi Tombol Redundan/Duplikat & Penyederhanaan Header Alur Telegram
- Apa yang salah: Terdapat tombol-tombol yang redundan di halaman detail proyek: tombol `Post Induk TG` di Quick Links menduplikasi tombol `Buka di TG ↗` di kartu Post Utama, dan tombol `Cek Update TG` / `Tarik Post Telegram` muncul 3 kali di lokasi berdekatan (topbar, wrapper header, dan header internal linimasa).
- Kenapa terjadi (root cause, bukan cuma gejala): Developer menambahkan tombol aksi di level page wrapper (`project-detail-client.tsx`) dan di Quick Links tanpa menyadari bahwa komponen child (`ProjectThreadView`) sudah mengintegrasikan header linimasa mandiri lengkap dengan tombol modal Telegram serta tombol link ke postingan asli di kartu utamanya.
- Perbaikan yang dilakukan: (1) Menghapus tombol `Post Induk TG` dari `ProjectQuickLinks`; akses postingan asli difokuskan pada kartu Post Utama sebagai `Buka di TG ↗`. (2) Menghapus tombol `Cek Update TG` dari top breadcrumb bar (hanya menyisakan tombol kembali dan `Edit Info`). (3) Menghapus wrapper header ganda di atas `ProjectThreadView` sehingga `ProjectThreadView` langsung menjadi konten utama kolom kiri. (4) Memastikan satu-satunya tombol `Cek Update TG` berada rapi di header internal linimasa.
- Aturan ke depan: JANGAN pernah menaruh tombol aksi yang sama di lebih dari satu tempat dalam satu layar; pusatkan tombol pembuka modal atau tautan eksternal pada komponen kontekstual yang paling relevan.

## [2026-09-10] Redesain Halaman Detail Proyek: Eliminasi Card Raksasa & Paradigma Checklist Mikro yang Membebani
- Apa yang salah: Halaman detail proyek memaksakan card hero raksasa yang membengkak (-463 baris) dan tab checklist tugas mikro (`InteractiveTaskList`) serta panduan terpisah yang tidak dibutuhkan oleh airdrop hunter.
- Kenapa terjadi (root cause, bukan cuma gejala): Developer mencoba memadukan paradigma to-do list umum ke dalam halaman detail airdrop, menduplikasi tombol status dan alarm di banyak card terpisah, serta menyembunyikan postingan asli Telegram di balik tab navigator yang kaku. Padahal airdrop hunter hanya butuh postingan Telegram asli (yang sudah berisi langkah lengkap) dan linimasa pembaruannya.
- Perbaikan yang dilakukan: (1) Mengeliminasi total card hero raksasa dan menyatukan eksekusi 1-klik (`Tandai Selesai Hari Ini`, `Snooze / Tunda`, countdown reset) ke bilah aksi ringkas di bawah identitas proyek. (2) Menghapus tab kaku dan `InteractiveTaskList`, menjadikan `ProjectThreadView` (Postingan Asli & Linimasa Telegram) sebagai konten utama langsung di kolom kiri. (3) Menyatukan pengaturan ritme (Harian/Mingguan/1x) dan alarm jam ke dalam widget ringkas di sidebar kanan tanpa icon ganda.
- Aturan ke depan: Untuk detail proyek airdrop, jangan paksakan checklist tugas mikro jika postingan sumber Telegram sudah utuh; jadikan thread Telegram sebagai konten utama yang langsung terlihat dan letakkan aksi eksekusi harian secara ringkas di bilah atas.

## [2026-09-10] Eliminasi Icon Ganda (Double Icon) & Standarisasi Lucide SVG Tanpa Emoji Unicode
- Apa yang salah: Elemen CustomSelect dan tombol aksi di beberapa modal (Edit Proyek, Tambah Proyek, Review Proyek, Tambah Task, Detail Proyek, Tasks) menampilkan dua icon bertumpuk/ganda (misal icon kalender Lucide berdampingan dengan emoji `⚡ Check-in Harian` atau `⭐ Priority Active`).
- Kenapa terjadi (root cause, bukan cuma gejala): Developer sebelumnya menyisipkan properti `icon` pada opsi `CustomSelect` sekaligus menaruh emoji unicode di awal string `label`, serta di tombol aksi menaruh komponen icon Lucide berdampingan dengan teks yang juga diawali emoji, sehingga kedua icon ter-render bersamaan.
- Perbaikan yang dilakukan: (1) Menghilangkan 100% emoji unicode dari teks label opsi CustomSelect, segmented control, tooltip, dan banner di seluruh proyek. (2) Mengganti seluruh icon opsi dropdown dengan icon Lucide SVG murni (`Clock`, `Repeat`, `CheckSquare`, `Play`, `Hourglass`, `Gift`, `CheckCircle2`, `PauseCircle`, `Folder`, `FolderOpen`, `Star`). (3) Menambahkan import `Globe` yang hilang di `create-project-modal.tsx` dan memverifikasi type-check serta build 100% lolos tanpa error.
- Aturan ke depan: JANGAN pernah menaruh emoji unicode di dalam string label atau teks tombol yang sudah memiliki properti/komponen icon Lucide; selalu gunakan HANYA komponen icon Lucide SVG murni untuk antarmuka yang bersih dan profesional.

## [2026-09-10] Pemisahan Konsep Antrean To-Do List Harian vs Alarm Waktu Spesifik (Time-Sensitive Alert)
- Apa yang salah: Pengguna bingung mengapa proyek harian tetap diingatkan tiap hari di dashboard padahal widget pengingat bertuliskan "Pengingat (0) - Belum ada pengingat terjadwal". Ketiadaan perbedaan visual dan dampak fungsional membuat fitur pengingat terasa redundan dengan status rutinitas harian.
- Kenapa terjadi (root cause, bukan cuma gejala): Fitur Dashboard to-do list (antrean kerja otomatis berbasis ritme `task_type`) dan Alarm (`reminders`) dicampuradukkan dengan nama yang sama ("Pengingat"). Akibatnya pengguna mengira harus memasang pengingat agar proyek muncul di to-do list, dan tab "Telat/Overdue" di Dashboard rancu karena menganggap semua proyek harian telat di pagi hari.
- Perbaikan yang dilakukan: (1) Memisahkan peran: Ritme Harian/Mingguan adalah antrean To-Do List otomatis di Dashboard (fleksibel kapan saja sebelum reset 07:00 WIB), sedangkan Pengingat dialihfungsikan menjadi Alarm Waktu Spesifik (misal: faucet jam 14:00 WIB / deadline snapshot). (2) Tab "Telat (Overdue)" di Dashboard kini HANYA dipicu oleh proyek yang memiliki alarm jam tertentu dan waktu saat ini sudah melewati jam alarm tersebut (`isReminderPastDue`). (3) Menambahkan tab filter "🔔 Alarm Jam" di Dashboard dan badge visual kontras (Emas untuk Alarm Jam vs Abu-abu tenang untuk Harian Fleksibel). (4) Merombak widget detail proyek menjadi "Alarm Waktu & Event" dengan status antrean to-do list yang transparan dan melegakan.
- Aturan ke depan: Bedakan secara visual dan fungsional antara Antrean To-Do List otomatis (bebas/fleksibel sebelum 07:00 WIB) dengan Alarm Waktu Spesifik; status "Telat/Overdue" HANYA boleh dipicu jika jam alarm spesifik telah terlewati.

## [2026-09-09] Sinkronisasi State Optimistik "Tandai Selesai" Dashboard & Pembersihan Zombie Service Worker
- Apa yang salah: (1) Menekan tombol "Tandai Selesai" di Dashboard menampilkan toast sukses namun tombol tetap "Tandai Selesai" dan proyek tidak berpindah ke tab "Selesai Hari Ini". (2) Muncul error `The FetchEvent for ... resulted in a network error response: the promise was rejected. sw.js:1 Uncaught (in promise) TypeError: Failed to convert value to 'Response'` di console browser.
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Fungsi `handleMarkProjectDone` di `dashboard-client-view.tsx` hanya memanggil `setTasks` tanpa memanggil `setProjects` (sehingga `social_links.last_daily_completed_at` tidak pernah terisi di React state), dan pada `setTasks` lupa menyertakan field `completed_at` (sehingga `isProjectDailyDone` terus mengevaluasi `false` saat re-render). (2) Terdapat Service Worker lawas (`sw.js`) yang tertinggal di cache browser untuk domain `dataeconomy.web.id` yang mencegat request dashboard dan gagal merespons.
- Perbaikan yang dilakukan: (1) Menyempurnakan `handleMarkProjectDone` dan `handleToggleDaily` untuk mengupdate `projects` (`last_daily_completed_at`) dan `tasks` (`status` & `completed_at`) secara optimistik seketika serta menuntaskan await ke `toggleProjectDailyTask`. (2) Memperluas toleransi `isProjectDailyDone` agar memeriksa `completed_at` maupun `updated_at`. (3) Menambahkan skrip pembersihan otomatis untuk mencabut semua registrasi Service Worker zombie pada `app/layout.tsx`.
- Aturan ke depan: State optimistik penyelesaian harian WAJIB memperbarui `projects` (social_links.last_daily_completed_at) dan `tasks` (completed_at) secara simultan, dan jangan biarkan zombie service worker mencegat fetch pada domain deployment.

## [2026-09-09] TypeError bgClass pada StatusBadge Akibat Normalisasi Status ready_to_claim dan Ketiadaan Fallback
- Apa yang salah: Halaman `/dashboard` mengalami crash fatal dengan layar putih "Application error: a client-side exception has occurred" dan console browser menunjukkan `Uncaught TypeError: Cannot read properties of undefined (reading 'bgClass')`.
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Status proyek `ready_to_claim` dinormalisasi menggunakan `.replace("_", "-")` yang hanya mengganti underscore pertama sehingga menghasilkan `"ready-to_claim"`, bukan key `"ready-claim"` pada konfigurasi `statusConfig`. (2) Komponen `StatusBadge` mengakses `statusConfig[status]` secara langsung tanpa object fallback aman, sehingga status yang tidak cocok mengembalikan `undefined` dan menyebabkan runtime crash saat membaca `config.bgClass`. (3) Belum adanya error boundary Next.js (`error.tsx`) pada root app router membuat error client-side merembet ke seluruh layar.
- Perbaikan yang dilakukan: (1) Menambahkan alias menyeluruh pada `statusConfig` (`ready_to_claim`, `ready-to-claim`, `ready-to_claim`, `in_progress`, `not_started`, dll.) dan fuzzy keyword matching. (2) Menambahkan fallback aman `defaultFallbackConfig` agar `config` tidak pernah bernilai `undefined`. (3) Membersihkan pemanggilan `.replace("_", "-")` di Dashboard, Projects, Tasks, dan Modal agar meneruskan status proyek secara langsung. (4) Menambahkan client error boundary `app/(app)/error.tsx` dan `app/error.tsx`.
- Aturan ke depan: Komponen pemetaan status/badge WAJIB selalu menyediakan fallback config aman terhadap input undefined/unknown dan mendukung format raw DB enum tanpa bergantung pada fungsi manipulasi string parsial.

## [2026-09-09] Audit Dead Code: Import Removal Harus Diverifikasi Terhadap JSX dan Seluruh File
- Apa yang salah: (1) Menghapus import `Input` dari `wallets-client-view.tsx` padahal komponen tersebut masih dipakai 7x di JSX modal form. (2) Menghapus import `t` dari `notification-popover.tsx` padahal masih dipakai di `aria-label`. (3) Rename param `projectName` → `_projectName` padahal param tersebut dipakai di JSX placeholder input. (4) Fungsi dead `_handleConvertToProject` di waitlist masih mereferensi state dan import yang sudah dihapus.
- Kenapa terjadi (root cause, bukan cuma gejala): Cleanup dilakukan file-per-file secara cepat menggunakan lint warnings tanpa melakukan grep/search menyeluruh di JSX body dan seluruh file untuk memastikan item yang dihapus benar-benar tidak dipakai di tempat lain selain deklarasi.
- Perbaikan yang dilakukan: Re-add `Input` import, ganti `t()` dengan `isEn` ternary, rename `_projectName` kembali ke `projectName`, hapus dead `_handleConvertToProject`. Verifikasi: lint 0 error/warning, tsc exit 0, build exit 0.
- Aturan ke depan: Sebelum menghapus import/state/fungsi saat cleanup, SELALU grep seluruh file (bukan hanya bagian import) untuk memastikan tidak ada referensi di JSX, string template, atau callback.

## [2026-09-09] Redesain Lapang Atur Pengingat Cepat & Penyelarasan Menyeluruh Status Siklus Hidup dan Rutinitas Airdrop
- Apa yang salah: (1) Area "Atur Pengingat Cepat" pada modal review proyek (`ProjectReviewModal`) terlihat sangat padat dan terkompresi karena berada di dalam modal sempit `max-w-2xl` dengan kolom berhimpitan. (2) Konsep status garapan sebelumnya rancu antara rutinitas harian/mingguan/1x jalan dengan fase siklus hidup airdrop (testnet berjalan, tutup/snapshot, siap klaim token, dan selesai klaim). Pengguna kesulitan menandai airdrop yang sudah selesai snapshot agar tidak buang-buang gas fee, atau airdrop yang link klaimnya sudah rilis.
- Kenapa terjadi (root cause, bukan cuma gejala): Pemisahan domain model antara *Operational Routine* (`task_type`: daily streak, weekly volume, 1-time set & forget) dan *Lifecycle Status* (`status`: in_progress, waiting/snapshot, ready_to_claim, completed) belum diekspresikan secara harmonis di seluruh titik sentuh (modal tambah proyek, modal edit proyek, modal review telegram, direktori tugas `/tasks`, dan halaman detail proyek `/projects/[id]`).
- Perbaikan yang dilakukan: (1) Melebarkan modal review menjadi `max-w-5xl` dan meredesain area "Atur Pengingat Cepat" menjadi grid lapang 4-kolom (`grid-cols-2 md:grid-cols-4 gap-3`) dengan pill frekuensi, waktu, dan toggle push browser. (2) Mengintegrasikan selektor Tipe Rutinitas (`⚡ Harian`, `🔄 Mingguan`, `🎯 1x Selesai`) dan Status Siklus Hidup 1-klik di seluruh halaman (`CreateProjectModal`, `EditProjectModal`, `ProjectReviewModal`, `tasks-client-view.tsx`, dan `project-detail-client.tsx`). (3) Menambahkan field `claim_url` untuk portal klaim / checker alokasi. (4) Menyediakan banner kontekstual peringatan tegas: banner ungu `⛔ Fase Testnet Berakhir / Menunggu Snapshot — STOP buang gas/tx` dengan tombol pemindahan ke Siap Klaim, banner amber `🎁 Airdrop Siap Diklaim` dengan tombol langsung buka portal klaim & tandai selesai, dan banner hijau `🏆 Airdrop Sukses Diklaim & Selesai`. (5) Memperbarui filter tab di `/tasks` dengan counter dinamis untuk semua status.
- Aturan ke depan: Bedakan secara tegas antara Tipe Rutinitas Pengerjaan (`daily`, `weekly`, `one_time`) dengan Status Siklus Hidup Airdrop (`in_progress`, `waiting`, `ready_to_claim`, `completed`); sediakan toggle 1-klik instan di kartu dan halaman detail serta peringatan stop gas fee saat snapshot.

## [2026-09-09] Eliminasi Tombol Ambigu "Batal Join" vs "Hapus" pada Kartu Waitlist
- Apa yang salah: Kartu pada tab "Waitlist yang Saya Ikuti" menampilkan tombol "Batal Join" berdampingan dengan tombol "Hapus", membingungkan pengguna karena secara visual kedua tombol sama-sama membuat kartu menghilang dari daftar tampilan saat ini.
- Kenapa terjadi (root cause, bukan cuma gejala): Developer sebelumnya membedakan aksi ubah status ke pending (`updateWaitlistStatus(id, "pending")`) dengan hapus DB (`deleteWaitlist(id)`), namun menaruh keduanya secara bersamaan di antarmuka tanpa menyadari bahwa dari sudut pandang pengguna yang sedang mengelola garapan, kedua tombol memiliki dampak visual yang identik dan menimbulkan kebingungan keputusan.
- Perbaikan yang dilakukan: (1) Menghapus tombol dan fungsi `handleRevertToPending` ("Batal Join") sepenuhnya dari kartu waitlist yang diikuti. (2) Menyederhanakan tindakan menjadi satu aksi tunggal yang jelas: Hapus (`Trash2` icon) dengan konfirmasi nama proyek yang eksplisit (`"Hapus waitlist [Nama]?"`). (3) Memeriksa seluruh halaman aplikasi lain (Feed, Projects, Tasks, Wallets, Dashboard) untuk memastikan tidak ada lagi pasangan tombol ambigu serupa.
- Aturan ke depan: JANGAN pernah meletakkan dua aksi destruktif/penghilang entitas yang mirip (seperti "Batal/Reset" berdampingan dengan "Hapus") pada kartu atau baris yang sama; gunakan satu aksi hapus tunggal yang jelas dan terkonfirmasi.

## [2026-09-09] Kartu Ringkas Waitlist yang Sudah Diikuti Tanpa Tutorial Pendaftaran
- Apa yang salah: Kartu waitlist di tab "Waitlist yang Saya Ikuti" menampilkan seluruh daftar preview tutorial tugas (3-5 baris bullet point instruksi pendaftaran) dan subtitle panjang, membuat tinggi kartu memanjang (~460px) dan memakan banyak ruang vertikal layar pengguna yang sudah bergabung.
- Kenapa terjadi (root cause, bukan cuma gejala): Layout kartu waitlist sebelumnya disatukan secara monolitik antara status eksplorasi (pending) dan status sudah diikuti (joined), padahal setelah mendaftar pengguna hanya membutuhkan nama garapan, tautan langsung, dan identitas akun yang dipakai.
- Perbaikan yang dilakukan: Memisahkan rendering kartu menjadi dua varian: (1) Untuk status `joined`, membuat kartu ultra-ringkas (~155px tinggi, hemat 65% ruang) yang membuang seluruh preview tutorial tugas, menonjolkan nama airdrop, baris Akun Terdaftar (lengkap dengan 1-klik copy dan tombol edit), baris Link airdrop (klik buka tab baru dan 1-klik copy), serta tombol `Update TG` & `+ Proyek` yang kompak. (2) Untuk status `pending` di tab Eksplorasi, tetap mempertahankan preview instruksi pendaftaran agar user tahu syarat tugas sebelum bergabung.
- Aturan ke depan: Untuk entitas yang telah selesai/diikuti oleh pengguna (seperti waitlist joined), buang instruksi langkah awal/tutorial dari tampilan kartu utama dan pertahankan hanya data esensial (nama, tautan, identitas akun terdaftar, dan aksi lanjutan).

## [2026-09-09] Penggantian Total Dropdown Native Browser dengan CustomSelect & Audit Alur Kerja Form
- Apa yang salah: (1) Elemen `<select>` bawaan browser (OS native) menampilkan popup biru/putih terang yang merusak tema gelap aplikasi dan kaku di perangkat mobile/desktop. (2) Form tambah akun sosial di `/wallets` dan proyek tidak menyediakan input platform kustom saat memilih "Lainnya/Custom", sehingga nama platform tidak tersimpan. (3) Modal transfer waitlist tidak menangani kondisi ketika belum ada proyek yang dibuat.
- Kenapa terjadi (root cause, bukan cuma gejala): Dropdown HTML `<select>` sebelumnya digunakan langsung di 11 file tanpa komponen UI terpusat, dan modal form berasumsi opsi platform selalu statis tanpa input dinamis tambahan.
- Perbaikan yang dilakukan: (1) Membangun `CustomSelect` (`components/ui/select.tsx`) bertema dark obsidian, beranimasi halus, berfitur smart auto-flip (mencegah terpotong batas layar/modal), navigasi keyboard (Escape, panah, Enter), dan dukungan icon/badge. (2) Mengganti 100% (19 dari 19) dropdown native browser di seluruh aplikasi (Wallets, Waitlist, Projects, Detail Proyek, Settings, serta modal Create Project, Edit Project, Create Task, Review Project, Set Reminder, dan Task List). (3) Memperbaiki alur form: menambahkan input customPlatform dinamis di modal Wallets & Create Project, menambahkan safe empty state pada transfer waitlist, dan mengeliminasi mutasi DOM langsung pada bulk select.
- Aturan ke depan: JANGAN pernah menggunakan elemen HTML `<select>` native bawaan browser; selalu gunakan `CustomSelect` dari `components/ui/select.tsx` untuk menjaga konsistensi visual tema gelap dan interaktivitas.

## [2026-09-09] Inline Editing Tanpa Popup untuk Wallets & Akun Sosial
- Apa yang salah: Data wallet (label, chain, address) dan akun sosial (label, handle, notes) yang sudah ditambahkan di `/wallets` tidak dapat diedit kembali jika ada kesalahan ketik atau pembaruan handle, memaksa pengguna menghapus dan membuat ulang atau menghadapi modal form popup yang memperlambat flow kerja.
- Kenapa terjadi (root cause, bukan cuma gejala): Halaman `/wallets` awalnya dirancang hanya dengan form tambah via modal dan tombol hapus/salin, tanpa mekanisme mutasi inline atau form edit in-place.
- Perbaikan yang dilakukan: (1) Membuat helper `updateUserAccount` di `lib/supabase/user-accounts.ts` yang menyinkronkan tabel Postgres `user_accounts` dan fallback `auth.user_metadata`. (2) Membuat komponen `InlineEditable` di `wallets-client-view.tsx` dengan transisi mulus, autofocus, deteksi blur (klik di area luar) untuk auto-save, Enter key untuk simpan cepat, Escape key untuk pembatalan, dan animasi feedback "Tersimpan ✓" hijau tanpa modal atau pop-up. (3) Menerapkan inline editing pada seluruh kolom kartu wallet (label, chain, address) dan kartu akun sosial (nama akun, handle, catatan).
- Aturan ke depan: Untuk edit data cepat pada dashboard/workspace airdrop, prioritaskan inline editing dengan auto-save on blur/Enter daripada pop-up modal konfirmasi yang memakan langkah klik tambahan.

## [2026-09-09] Proteksi Hapus Proyek Prioritas, Tanpa Proteksi Skip, dan Alih Fungsi Ikon Lonceng Header
- Apa yang salah: (1) Garapan airdrop tier-1/prioritas rentan terhapus secara tidak sengaja ketika pengguna melakukan aksi Hapus Massal (Bulk Delete) di daftar proyek atau salah klik di modal edit. (2) Rute `/reminders` masih ada dan ikon lonceng header mengarah ke rute tersebut, padahal manajemen pengingat sudah terintegrasi di dalam proyek & dashboard.
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Fitur bulk delete sebelumnya langsung menghapus seluruh array `selectedProjectIds` tanpa memeriksa bobot kepentingan/status garapan. (2) Navigasi lonceng header belum dialihfungsikan menjadi notification center terintegrasi dan masih mengandalkan rute halaman mandiri `/reminders`.
- Perbaikan yang dilakukan: (1) Mengimplementasikan fitur Prioritas 1-Klik (`is_priority`) di Dashboard, Explorer Proyek, dan Detail Proyek. (2) Menerapkan Proteksi Kunci Hapus (Delete Lock Safeguard): proyek prioritas otomatis dikecualikan dari bulk delete dan tombol hapus di modal edit dikunci dengan ikon lock, namun fitur "Lewati Hari Ini" (skip) tetap dibiarkan aktif bebas tanpa proteksi skip sesuai preferensi alur kerja airdrop hunter. (3) Menghapus rute `/reminders` beserta middleware-nya, dan mengalihfungsikan ikon lonceng header menjadi `NotificationPopover` yang menyajikan status tugas hari ini, garapan prioritas, dan tombol cepat "✓ Selesai" tanpa berpindah halaman.
- Aturan ke depan: Garapan berstatus prioritas WAJIB dilindungi dari aksi penghapusan (baik massal maupun satuan) tanpa menghalangi aksi operasional harian (seperti skip/tunda), dan ikon lonceng header wajib beroperasi sebagai flyout notification popover interaktif.

## [2026-09-09] Bug Fallback projects.slice(0, 5) Merusak Filter Tab "Siap Dikerjakan" di Dashboard
- Apa yang salah: Saat pengguna telah menyelesaikan satu tugas ("Selesai Hari Ini") dan melewati tugas lainnya ("Dilewati Hari Ini"), tab filter "Siap Dikerjakan" (counter 0) tetap menampilkan kedua proyek tersebut bukannya kosong.
- Kenapa terjadi (root cause, bukan cuma gejala): Pada switch `displayedProjects` di `dashboard-client-view.tsx`, baris `case "ready"` menggunakan ternary fallback: `return readyProjects.length > 0 ? readyProjects : projects.slice(0, 5);`. Ketika `readyProjects` benar-benar kosong (karena semua tugas sudah beres/dilewati), kode justru jatuh ke fallback dan merender 5 proyek pertama dari database.
- Perbaikan yang dilakukan: Menghapus fallback `projects.slice(0, 5)` dan langsung mengembalikan `readyProjects` secara murni (`return readyProjects;`). Serta menambahkan ucapan celebratory & deskripsi ramah pada empty state filter siap dikerjakan ("Luar biasa! Semua tugas hari ini sudah selesai atau dilewati 🎉").
- Aturan ke depan: JANGAN pernah menambahkan fallback array non-kosong (`projects.slice(...)`) pada state filter yang sah-sah saja bernilai kosong (0 item); biarkan filter mengembalikan array kosong agar komponen Empty State dapat dirender dengan benar.

## [2026-09-09] Tombol Cepat (Quick Picker) Identitas di Modal Waitlist & Dual-Layer Penyimpanan Akun Sosial
- Apa yang salah: (1) Modal "Catat Pendaftaran Waitlist" mewajibkan pengguna mengetik atau copy-paste manual alamat wallet/email/handle sosial setiap kali mencatat waitlist, padahal data wallet sudah tersimpan di sistem. (2) Halaman "Wallets & Akun" (`/wallets`) hanya menyediakan form untuk crypto wallet address dan tidak memiliki tempat untuk mencatat akun media sosial (Twitter/X, Telegram, Discord, Email).
- Kenapa terjadi (root cause, bukan cuma gejala): Fitur waitlist dan wallets awalnya dikembangkan terisolasi; skema tabel `accounts` lama terikat foreign key `project_id` dan belum ada skema identitas personal tingkat user (`user_id`) yang dapat diakses lintas modul.
- Perbaikan yang dilakukan: (1) Menambahkan tab "Akun & Sosial Media" di `/wallets` dengan dukungan platform X, Discord, Telegram, Email, GitHub, Google, tombol 1-klik catat email login, dan perlindungan privasi non-sensitif. (2) Menerapkan dual-layer architecture pada `user-accounts.ts` (tabel Postgres `user_accounts` + fallback instan ke Supabase `auth.user_metadata`) sehingga fitur langsung aktif 0-error tanpa menunggu migrasi manual. (3) Menambahkan bilah Tombol Cepat (Quick Picker) di modal pendaftaran waitlist yang menampilkan pills wallet tersimpan, wallet terkoneksi browser, akun sosial, dan email dengan 1-klik pengisian instan serta indikator centang aktif.
- Aturan ke depan: Untuk formulir pencatatan identitas/akun airdrop, selalu sediakan tombol cepat (quick picker pills) dari identitas yang sudah tersimpan, dan gunakan strategi dual-layer (DB table + user_metadata fallback) untuk data profil/identitas pengguna.

## [2026-09-09] Audit UX Tombol Ambigu: Eliminasi Reset Feed, Kelompokkan Proyek, dan Redundansi Aksi
- Apa yang salah: (1) Tombol "Reset Status" pada kartu feed membingungkan user karena mengembalikan feed jadi belum digarap padahal proyek aslinya tetap ada di workspace. (2) Tombol "Kelompokkan Proyek" di feed disalahpahami fungsinya karena sebenarnya hanya menyaring postingan (deduplikasi), bukan membuat folder grup. (3) Tombol di footer modal pencarian Telegram waitlist ("Pindahkan Waitlist Ini ke Tugas Droppr") ambigu terhadap tombol "+ Proyek" di kartu. (4) Terdapat inkonsistensi label tombol ("Cari Update TG" vs "Cek Update TG") di halaman detail proyek.
- Kenapa terjadi (root cause, bukan cuma gejala): Fitur-fitur lama meninggalkan sisa tombol debugging/manual override yang bertentangan dengan arsitektur sinkronisasi otomatis (`syncFeedsWithProjects`) dan membuat bilah kontrol (search bar & modal footer) menjadi sempit serta membingungkan.
- Perbaikan yang dilakukan: (1) Menghapus tombol reset status di feed; kartu yang sudah diimpor kini permanen menampilkan badge `[ ✓ Sudah Jadi Proyek ]` dan tautan langsung `[ Buka Proyek ↗ ]`. (2) Menghapus tombol "Kelompokkan Proyek" sehingga bilah pencarian lebih lega dan nyaman. (3) Menghapus tombol footer ambigu di modal Telegram waitlist dan menggantinya dengan catatan sumber data serta tombol tutup yang bersih. (4) Menyeragamkan penamaan tombol Telegram di seluruh proyek menjadi "Cek Update TG" (`Check TG Updates`).
- Aturan ke depan: Jangan menambahkan tombol manual reset pada entitas yang memiliki sinkronisasi dua arah otomatis dengan database, hindari tombol filter dengan label aksi folder ("Kelompokkan"), dan pastikan tombol modal pencarian murni hanya menangani hasil pencarian tersebut.

## [2026-09-09] Pencegahan False Positive Scanner Telegram Waitlist & Perbaikan Kontras Badge Counter
- Apa yang salah: (1) Fitur pemindaian Telegram massal (Batch Scanner) membaca postingan asli pendaftaran waitlist sebagai "● 1 Baru" update, sehingga waitlist (seperti Pawffle, Goalhood, AMMORA) memicu notifikasi update baru palsu padahal isinya adalah postingan yang sama dengan waitlist itu sendiri. (2) Tombol "Update TG" di kartu waitlist menampilkan bulatan hijau/teal polos tanpa angka yang terlihat ("icon di sisi update tg emang cuma hijau tidak ada angka nya?").
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Pada `telegram-batch-scanner.ts`, `fetchExistingSourceUrls` hanya memeriksa tabel `project_updates` dan `projects`, tidak memeriksa tabel `waitlists` maupun `source_url` waitlist; serta `scanTargets` tidak menyertakan `source_url` dan `raw_text` waitlist sehingga scanner memperlakukan postingan pendaftaran asal sebagai postingan update baru. (2) Pada badge tombol "Update TG", class warna teks ditulis `text-text-inverse` yang tidak terdefinisi di Tailwind config, sehingga angka mewarisi `text-link-teal` dari tombol parent di atas background `bg-link-teal` (warna font dan background identik teal pada teal, membuat angka 100% tidak kasat mata), ditambah pemakaian class invalid `py-0.2`.
- Perbaikan yang dilakukan: (1) Menambahkan `sourceUrl` dan `rawText` ke `ProjectScanTarget`, mengecek `source_url` dari tabel `waitlists`, dan menerapkan filter ketat pada scanner agar mengecualikan `rootUrls` serta mendeteksi kesamaan teks (`arePostsSimilar(item.text, rootText)`). (2) Di modal Update TG, menambahkan badge pembeda jelas antara "Postingan Pendaftaran Asli" dan "Update Lanjutan". (3) Mengganti `text-text-inverse` menjadi `text-on-accent` (`#14181F`) dengan padding valid `py-0.5` dan format `+{count}` sehingga angka update (seperti `+1`, `+3`) tampil kontras dan tajam di atas latar teal/amber.
- Aturan ke depan: Scanner Telegram wajib selalu memvalidasi URL dan kesamaan teks postingan terhadap postingan asal proyek/waitlist untuk mencegah self-update false positive, dan setiap badge angka wajib menggunakan token kontras semantik yang sah (`text-on-accent` / `text-bg-base`).

## [2026-09-09] Sinkronisasi Status Hapus Thread Proyek & Integritas Postingan Asli Telegram
- Apa yang salah: (1) Menghapus item update di thread linimasa proyek tetap memunculkan status "Sudah Ada di Thread Proyek" pada modal pencarian Telegram, dan data di fallback `social_links.thread_updates` tidak terhapus. (2) Menambahkan update dari modal Telegram memotong paksa teks pesan menjadi hanya 400 karakter dan memecah judul secara aneh. (3) Keberadaan tombol "Rapihkan dengan AI" dan pemisahan tombol aksi (+ Langkah Task / + Catatan Berita) membuat alur pengerjaan tidak natural.
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Fungsi `handleDeleteThread` di `ProjectThreadView` hanya mengubah state lokal tanpa mengabari parent (`project-detail-client.tsx`), dan fungsi `deleteProjectThread` melakukan early return `if (!error) return` sebelum membersihkan fallback `social_links.thread_updates`. (2) `telegram-update-modal.tsx` memanggil `lines.slice(1).join("\n").slice(0, 400)` yang memangkas pesan asli Telegram.
- Perbaikan yang dilakukan: (1) Menyambungkan callback `onThreadsChange` dari `ProjectThreadView` ke parent `threads` state, menambahkan re-fetch real-time saat modal dibuka, dan memastikan `deleteProjectThread` menghapus dari tabel Postgres sekaligus membersihkan `social_links.thread_updates`. (2) Menghapus pemotongan `.slice(0, 400)`, menyimpan 100% teks postingan asli Telegram, dan merendernya secara utuh dengan tanggal & jam rilis asli yang jelas (`formatFullDate`). (3) Menghapus tombol dan mode "Rapihkan dengan AI", mengganti tombol aksi menjadi tombol tunggal "+ Tambahkan", serta menyediakan opsi pembatalan/penghapusan langsung di dalam modal.
- Aturan ke depan: Setiap perubahan data thread di child component WAJIB disinkronkan ke state parent melalui callback, fungsi delete database wajib membersihkan tabel dan fallback sekaligus, dan postingan Telegram yang ditambahkan ke linimasa wajib disimpan utuh tanpa pemotongan karakter.

## [2026-09-09] Hardcoded Dark Background Hex Menyebabkan Bocor Warna Gelap & Kontras Rusak di Mode Terang
- Apa yang salah: Modal tutorial tugas proyek (`today-task-guide-modal.tsx`) dan beberapa dialog lainnya menampilkan area hitam pekat `#0c1017` di dalam modal putih pada mode terang (light mode), serta backdrop overlay yang terlalu gelap (85% black) dan input autofill yang menghitam.
- Kenapa terjadi (root cause, bukan cuma gejala): Dialog box dan beberapa container masih menggunakan hardcoded dark Tailwind classes seperti `bg-[#0c1017]/90`, `bg-[#0e131b]`, `bg-[#0d121b]`, dan `bg-[#07090E]/85` alih-alih token semantik (`bg-bg-elevated`, `bg-bg-base`, `border-border-hairline`). Ketika aturan global mode terang menimpa card dalam menjadi putih, warna hitam dasar dialog pembungkus tembus keluar sehingga modal tampak belang dan teks gelap menjadi tidak terbaca.
- Perbaikan yang dilakukan: (1) Mengubah `today-task-guide-modal.tsx`, `components/ui/modal.tsx`, `feed-client-view.tsx`, `community-credits-modal.tsx`, `create-project-modal.tsx`, `edit-project-modal.tsx`, dan floating action/toast bars menjadi token semantik penuh (`bg-bg-elevated`, `bg-bg-base`, `border-border-hairline`, `text-text-primary`). (2) Menambahkan safety-net komprehensif di `styles/globals.css` untuk mode terang yang menetralkan semua class hex dark tersisa, meredupkan backdrop overlay menjadi soft dim (`rgba(20, 24, 31, 0.35)`), membersihkan background `<option>`, dan memperbaiki browser autofill agar berlatar putih bersih.
- Aturan ke depan: Jangan pernah menggunakan class warna hex gelap hardcoded (`bg-[#...]`) pada elemen UI yang mendukung tema ganda; selalu gunakan token semantik CSS variables (`bg-bg-elevated`, `bg-bg-base`, `border-border-hairline`).

## [2026-09-09] Duplikasi URL Link Tautan Telegram Karena Normalisasi Tag Anchor
- Apa yang salah: Tautan dalam pesan Telegram (misalnya link registrasi atau sumber tweet) muncul ganda seperti "https://hyperanon.org (https://hyperanon.org/)" di modal preview feed, panduan proyek, dan waitlist.
- Kenapa terjadi (root cause, bukan cuma gejala): Parser scraper Telegram sebelumnya mengonversi semua tag `<a href="$1">$2</a>` menjadi template `$2 ($1)`. Ketika teks tautan ($2) adalah URL yang sama persis dengan atribut href ($1) (hanya beda trailing slash atau protokol), hasilnya menjadi duplikasi link berdampingan.
- Perbaikan yang dilakukan: Membuat utilitas terpusat `lib/utils/clean-links.ts` (`normalizeUrlForCompare`, `cleanDuplicateLinks`, dan `cleanTelegramHtml`). Jika teks dan href mengarah ke tujuan yang sama, tautan tidak diduplikasi dan hanya menampilkan satu URL bersih. Utilitas ini diterapkan di semua rute scraper, fetch query, fungsi salin/terjemah, dan parser teks interaktif di feed, waitlist, panduan, dan thread linimasa.
- Aturan ke depan: Jangan pernah mengonversi tag anchor HTML ke format `teks (href)` tanpa memeriksa kesamaan URL antara teks dan href terlebih dahulu menggunakan fungsi pembersih tautan terpusat.

## [2026-09-09] Audit Menyeluruh Lokalisasi Bilingual (ID/EN) di Seluruh Halaman & Modal
- Apa yang salah: Beberapa tombol, modal aksi (seperti transfer tugas waitlist, telegram updates modal, quick links, status pills, badge, dan konfirmasi hapus), serta teks bantuan masih tertinggal dalam bahasa Indonesia ketika user mengganti bahasa sistem ke EN di Settings.
- Kenapa terjadi (root cause, bukan cuma gejala): Lokalisasi sebelumnya hanya difokuskan pada teks navigasi statis utama (`dictionaries/id.ts` & `en.ts`), sedangkan komponen-komponen fitur interaktif, modal sekunder, dan tooltip aksi dibuat dengan hardcoded bahasa Indonesia tanpa memanggil `useTranslation()`.
- Perbaikan yang dilakukan: (1) Menghubungkan hook `useTranslation()` (`locale`, `isEn`, `t`) ke seluruh komponen interaktif: `telegram-update-modal`, `project-thread-view`, `interactive-task-list`, `community-credits-modal`, `guide-viewer`, `project-quick-links`, `project-status-pills`, `status-badge`, dan modal-modal konfirmasi. (2) Mengonversi format tanggal dinamis (`formatDate`, `formatTime`) agar otomatis beralih antara `id-ID` dan `en-US`. (3) Memperluas input konfirmasi destruktif di Settings agar menerima kata kunci "HAPUS" maupun "DELETE".
- Aturan ke depan: Setiap penambahan komponen baru atau modal interaktif WAJIB mengonsumsi `useTranslation()` dan menyediakan teks bilingual (ID/EN) untuk semua tombol, placeholder, alert, dan badge status.

## [2026-09-09] Terjemahan Manual Bersyarat: Tidak Otomatis, Hanya Muncul Jika Bahasa Berlawanan dengan Bahasa Sistem
- Apa yang salah: Sebelumnya tombol terjemahan muncul untuk semua postingan tanpa memeriksa apakah bahasa postingan sudah sama dengan bahasa sistem pengguna, sehingga postingan bahasa Indonesia tetap ditawari tombol terjemahan ke bahasa Inggris untuk user lokal Indonesia.
- Kenapa terjadi (root cause, bukan cuma gejala): Logika pemilihan tombol sebelumnya hanya memeriksa bahasa postingan itu sendiri (`detected === "id"`), bukan relasi antara bahasa postingan dan bahasa aktif/sistem pengguna (`locale`).
- Perbaikan yang dilakukan: (1) `getTranslationAction` kini menerima `(text, userLang, isTranslated)`. Jika bahasa postingan sama dengan bahasa sistem user (misal user `id` & postingan `id`), terjemahan tidak diperlukan (`shouldShowTranslate = false`) sehingga tombol tidak dimunculkan sama sekali. (2) Postingan tidak pernah diterjemahkan secara otomatis — selalu tampil dalam bahasa aslinya. (3) Kartu-kartu feed airdrop tampil murni dalam teks asli, dan tombol terjemahan hanya disediakan on-demand di dalam modal "Lihat Postingan Asli" jika bahasa postingan berlawanan dengan bahasa sistem user.
- Aturan ke depan: Jangan pernah mengotomatisasi terjemahan postingan; tombol terjemahan hanya ditampilkan jika bahasa postingan berbeda/berlawanan dengan bahasa sistem user, dan menargetkan bahasa sistem user tersebut.

## [2026-09-09] Native HTML5 Drag & Drop Lebih Cepat dan Ringan daripada Library Eksternal untuk Explorer
- Apa yang salah: Menambah dependensi library DnD eksternal (seperti dnd-kit atau react-dnd) berisiko menambah bundle size (~30-50 kB) dan memicu re-render reaktif yang berat saat menggeser banyak kartu proyek.
- Kenapa terjadi (root cause, bukan cuma gejala): Fitur Drag & Drop yang dibutuhkan hanya pemindahan kartu proyek ke dropzone folder, yang sepenuhnya didukung oleh HTML5 Drag and Drop API bawaan browser dengan akselerasi GPU 60 FPS dan 0 kB bundle.
- Perbaikan yang dilakukan: Menggunakan native HTML5 Drag and Drop (`draggable`, `onDragStart`, `onDragOver`, `onDrop`) yang mendukung single dan multi-drag item terseleksi, efek visual highlight ring glow pada folder target, dan state update optimistik 0ms lag; serta floating action bar untuk aksi massal (Move, Archive, Delete).
- Aturan ke depan: Untuk interaksi drag-and-drop antar container/folder, selalu gunakan HTML5 Native DnD API bawaan browser sebelum mempertimbangkan library pihak ketiga.

## [2026-09-09] Reposisi Halaman Tasks Sebagai Direktori Status Operasional & Penghapusan Menu Reminders
- Apa yang salah: (1) Tab Reminders di sidebar menciptakan duplikasi fungsional karena pengingat sudah terintegrasi langsung di detail proyek dan dashboard. (2) Halaman Tasks sebelumnya membingungkan karena checklist mikro sudah ada di dashboard, dan airdrop hunter kesulitan mengetahui mana garapan yang sudah ditutup/menunggu snapshot agar tidak membuang gas fee dan waktu.
- Kenapa terjadi (root cause, bukan cuma gejala): Konsep checklist to-do list umum diterapkan tanpa melihat spesifikasi alur kerja airdrop hunter yang lebih membutuhkan klasifikasi operasional garapan: Rutin Berulang (daily/weekly transactions), Sekali Selesai (one-time form/claim role), Ditutup/Menunggu Snapshot (fase testnet selesai, berhenti garap), dan Siap Klaim Reward (TGE/claim token).
- Perbaikan yang dilakukan: (1) Menghapus entri `Reminders` dari navigasi sidebar (`sidebar.tsx`). (2) Merombak `TasksClientView` menjadi "Pusat Status Operasional Garapan" (Task Hub & Operational Directory) lengkap dengan filter kategori operasional (Rutin Berulang, Sekali Selesai, Ditutup/Menunggu Snapshot, Siap Klaim), filter pencarian nama/chain, toggle tipe tugas 1-klik (`updateProjectTaskType`), banner status informatif, dan tombol cepat penyelesaian harian.
- Aturan ke depan: Halaman Tasks di Droppr adalah direktori klasifikasi operasional garapan (bukan checklist mikro duplikasi dashboard), dan pengingat proyek dikelola secara kontekstual di dalam proyek masing-masing.

## [2026-09-09] Perbedaan Kritis: Tandai Selesai Tugas Harian vs Selesai Siklus Hidup Proyek
- Apa yang salah: Menekan tombol "Tandai Selesai" di halaman detail proyek sebelumnya mengubah `project.status` menjadi `completed` (Selesai Proyek/Klaim Selesai), bukannya menandai tugas garapan hari ini selesai yang besok harus dikerjakan lagi.
- Kenapa terjadi (root cause, bukan cuma gejala): Terjadi percampuran konsep antara Status Siklus Hidup Proyek Jangka Panjang (`project.status`: `not_started`, `in_progress`, `waiting`, `ready_to_claim`, `completed`) dengan Rutinitas Tugas Harian Airdrop (Daily Task / Check-in) yang terikat siklus reset harian (07:00 WIB / 00:00 UTC) dan harus tersinkronisasi bolak-balik dengan Command Center Dashboard.
- Perbaikan yang dilakukan: Membuat utilitas terpusat `daily-tasks-helper.ts` (`isProjectDailyDone` & `toggleProjectDailyTask`) yang melacak `social_links.last_daily_completed_at` dan tabel `tasks` terhadap siklus reset harian; tombol di halaman detail proyek kini khusus untuk "✓ Tugas Hari Ini Selesai (Buka Kembali)" dengan info "Reset: 07:00 WIB", tanpa mengubah `project.status`; di Dashboard ditambahkan tab filter "Selesai Hari Ini" beserta sinkronisasi tombol di kartu dan modal tutorial.
- Aturan ke depan: Jangan pernah mencampur status tugas harian dengan status lifecycle proyek (`project.status`); tugas harian selalu dievaluasi terhadap jam reset harian (07:00 WIB) dan disimpan di `last_daily_completed_at` serta tabel tasks.

## [2026-09-09] Penghapusan Total Double Info Sintetis: Wajib Murni Postingan Asli
- Apa yang salah: Kartu utama linimasa proyek menampilkan "double info", yaitu template sintetis lama (`### Panduan Garapan: ... #### Langkah Pengerjaan`) di atas dan teks postingan asli di bawahnya.
- Kenapa terjadi (root cause, bukan cuma gejala): Riwayat data lama di database menyimpan gabungan template sintetis dan teks asli dalam field `guide_content`, dan fungsi ekstraksi sebelumnya tidak memotong bagian template sebelum separator `#### 📄 Catatan / Pesan Asli Sumber`.
- Perbaikan yang dilakukan: Memperbarui fungsi `extractOriginalTelegramText` di `project-thread-view.tsx` dengan regex separator yang ketat sehingga seluruh bagian sebelum separator (template sintetis & langkah pengerjaan) dibuang 100%, dan kartu utama hanya merender murni teks postingan asli Telegram secara natural.
- Aturan ke depan: Untuk tampilan linimasa proyek, selalu tampilkan HANYA postingan asli sumber (buang seluruh template sintetis yang menduplikasi informasi).

## [2026-09-09] Penataan Tambah Proyek Manual via Link Telegram & Penghapusan Ekstraksi AI Kaku
- Apa yang salah: (1) Menambah proyek airdrop sebelumnya masih memaksakan ekstraksi AI yang lambat dan memecah kalimat judul, sponsor YouTube, dan nama channel menjadi puluhan checkbox kaku yang merusak halaman proyek. (2) Istilah "Thread" / "Balas Thread" terasa asing dan tidak natural bagi airdrop tracker. (3) Tidak ada cara cepat menerjemahkan postingan airdrop berbahasa asing tanpa AI token, dan baris tugas di halaman proyek menelan seluruh layar tanpa opsi hapus baris sponsor.
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Arsitektur awal berasumsi pengguna membutuhkan pemecahan otomatis seluruh kalimat pesan menjadi task database, padahal pengguna lebih membutuhkan postingan asli yang utuh dan hanya mencatat tugas-tugas riil. (2) Komponen modal tambah proyek mengarahkan user ke prompt AI alih-alih mengambil postingan langsung dari link publik Telegram. (3) Komponen linimasa tidak menyediakan tombol quick translate, tombol pembersih spam sponsor, atau collapsible task list.
- Perbaikan yang dilakukan: (1) Merombak `CreateProjectModal` dengan 2 opsi langsung: via Link Telegram publik (`/api/telegram/post`) dan Paste/Ketik Manual ($0 token, tanpa AI kaku), lengkap dengan tombol translasi instan satu klik (`/api/translate` via MyMemory API). (2) Menghilangkan istilah "Thread" / "Balas Thread" dan menggantinya dengan "Linimasa Garapan & Update", "Tambah Tugas", dan "Simpan Catatan". (3) Menambahkan tombol "Cek Update TG" persis di samping tombol kirim update di bar bawah. (4) Mendesain ulang checklist tugas menjadi compact list dengan tombol hapus satuan dan tombol "Hapus Sponsor" untuk membersihkan iklan/promo yang tidak sengaja tercatat.
- Aturan ke depan: Simpan postingan airdrop sebagai teks utuh terpusat (bukan memecah seluruh baris jadi checkbox liar), sediakan fitur translasi instan satu klik, dan selalu sediakan tombol hapus satuan serta tombol pembersih sponsor pada daftar checklist tugas.

## [2026-09-09] Siklus Update Berulang Nama Proyek di Telegram dan Scroll-Lock Modal Popup
- Apa yang salah: (1) Munculnya banyak entri dengan nama proyek yang sama di Feed Telegram membuat daftar terlihat berulang/duplikat, membingungkan pengguna yang ingin mencari garapan baru. (2) Keberadaan checkbox checklist di dalam kartu Feed mengacaukan tujuan utama halaman (Feed adalah tempat mencari sinyal baru, bukan tracker checklist). (3) Saat modal popup terbuka dan di-scroll, layer halaman di belakang popup ikut bergeser (background scroll bleed).
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Di Telegram, channel airdrop secara alami mengirimkan update berkala (faucet, phase 2, snapshot, reminder) untuk proyek yang sama sepanjang siklus hidup proyek. (2) Konsep checklist tasks dari Telegram parser secara berlebihan ditempelkan ke setiap kartu feed alih-alih difokuskan saat di-convert menjadi Project. (3) Komponen dialog modal tidak menerapkan penguncian style `overflow: hidden` pada `document.body` saat state terbuka.
- Perbaikan yang dilakukan: (1) Mengimplementasikan Smart Deduplication & Mention Frequency Counter (`extractCoreProjectKey` + toggle "Kelompokkan Proyek" dan badge "3x Update") sehingga update Telegram dihitung sebagai frekuensi dan dapat dikelompokkan secara ringkas. (2) Menghapus seluruh logic checkbox dari kartu Feed, mengubah desainnya menjadi kartu pesan Telegram Liquid Frosted Glass mewah dengan logo resmi channel sumber (`/images/credits/dutacrypto.webp` & `airdropfinder.webp`), cuplikan pesan elegan, tombol "Lihat Postingan Asli", dan tombol cepat "+ Kerjakan Jadi Proyek". (3) Menambahkan `useEffect` scroll-lock (`document.body.style.overflow = "hidden"`) serta `overscroll-contain` pada komponen `Modal` dan preview dialog. (4) Merombak tampilan `/waitlist` dengan bahasa desain Liquid Frosted Glass yang konsisten.
- Aturan ke depan: Untuk feed sinyal dari Telegram, jangan tampilkan checkbox checklist; kelompokkan update berulang dengan mention frequency counter dan sediakan toggle deduplikasi, serta selalu pastikan setiap komponen modal popup mengunci scroll body.

## [2026-09-09] Penyatuan guide_content Sintetis dengan Raw Text Menyebabkan Konten Tumpang-Tindih di Modal Panduan
- Apa yang salah: Memilih tab "Panduan Rapi" masih memunculkan bagian `📄 Catatan / Pesan Asli Sumber` beserta teks pesan mentah di bawahnya, dan memilih tab "Postingan Asli" malah menampilkan seluruh template panduan rapi sintesis (`### Panduan Garapan:`, `#### 🔗 Tautan Penting`).
- Kenapa terjadi (root cause, bukan cuma gejala): Fungsi `formatGuideContent` di `airdrop-parser.ts` menggabungkan markdown template sintetis dengan teks mentah Telegram ke dalam satu field string tunggal `guide_content`, sementara komponen modal dialog sebelumnya langsung me-render seluruh `project.guide_content` pada kedua tab tanpa memisahkan potongan sebelum dan sesudah separator `#### 📄 Catatan / Pesan Asli Sumber`.
- Perbaikan yang dilakukan: Membuat fungsi utilitas `extractGuideAndOriginalPost` yang mendeteksi separator pemisah dan memisahkan konten menjadi `formattedGuide` (hanya panduan terstruktur rapi) dan `originalPost` (murni teks postingan asli Telegram sumber); menyimpan `raw_text` secara independen di `social_links.raw_text`; dan menambahkan tombol "Salin Postingan" di tab Postingan Asli.
- Aturan ke depan: Ketika menyimpan konten gabungan (sintesis + raw source) ke satu kolom database, selalu sediakan parser pemisah dua arah atau simpan raw source di atribut JSONB terpisah agar antarmuka multi-mode dapat menampilkan konten masing-masing secara terisolasi tanpa bleed.

## [2026-09-08] Bug Status is_imported Terkunci dan Postgres 22P02 Error pada Konversi Feed Airdrop
- Apa yang salah: Beberapa feed airdrop tidak dapat ditambahkan menjadi proyek (tombol diam tanpa error message), dan proyek yang telah dihapus dari daftar garapan masih tetap berstatus "Sudah Jadi Proyek Garapan" di Feed tanpa opsi untuk membuat ulang.
- Kenapa terjadi (root cause, bukan cuma gejala): (1) Status `is_imported: true` disimpan statis di tabel `airdrop_feeds` dan tidak pernah di-revert ke `false` saat user menghapus proyek dari tabel `projects`. (2) Pada fungsi `convertFeedToProject`, string nama/konten/JSON links dari scraping Telegram tidak disanitasi dari low/unpaired unicode surrogates, memicu PostgreSQL error 22P02 invalid JSON surrogate yang membatalkan insert secara diam-diam tanpa feedback ke UI.
- Perbaikan yang dilakukan: Membuat fungsi rekonsiliasi dinamis `syncFeedsWithProjects` yang mencocokkan feed dengan proyek user yang aktif secara real-time dan mereset status feed yang proyeknya sudah dihapus; menambahkan sanitasi `sanitizeSurrogates` dan `sanitizeJsonObject` sebelum insert ke Supabase; menambahkan tombol aksi "Lihat Proyek" dan tombol "Reset" pada kartu feed yang sudah di-import; serta menambahkan revert `is_imported: false` saat menghapus proyek di modal edit proyek dan settings.
- Aturan ke depan: Status relasional antar tabel (seperti import/link) harus divalidasi dinamis terhadap eksistensi entitas induknya atau disinkronkan saat operasi delete, dan selalu sanitasi karakter unicode surrogate sebelum melakukan insert string/JSONB ke PostgreSQL.

## [2026-09-08] Layout Shell Hardcoded ml-64 dan Ketiadaan Mobile Drawer Navigasi
- Apa yang salah: Tampilan aplikasi rusak dan konten terpotong pada layar mobile karena sidebar fixed `w-64` menutupi layar dan area konten utama didorong margin kiri tetap `ml-64` (256px), serta topbar mengalami tabrakan elemen.
- Kenapa terjadi (root cause, bukan cuma gejala): Layout utama `app/(app)/layout.tsx` menggunakan class desktop statis tanpa breakpoint responsif (`ml-64` alih-alih `ml-0 md:ml-64`), komponen Sidebar tidak memiliki drawer state, backdrop overlay, atau tombol tutup mobile, serta Topbar tidak menyediakan tombol toggle hamburger.
- Perbaikan yang dilakukan: Membuat komponen `AppShell` (`components/features/app-shell.tsx`) yang mengontrol pembukaan drawer mobile dengan gesture esc/route change/backdrop click, mengubah Sidebar menjadi slide-over drawer di mobile dengan animasi transisi dan tombol close X, menambahkan tombol hamburger dan mini brand logo di Topbar, memadatkan tombol aksi (Tambah, Wallet, Filter pills), serta mengoptimalkan padding konten halaman dan touch targets checklist.
- Aturan ke depan: Semua shell layout dan navigasi wajib memiliki breakpoint mobile-first (`ml-0 md:ml-...`), sediakan mobile slide-over drawer dengan backdrop untuk navigasi sidebar, dan pastikan container filter selalu memiliki `overflow-x-auto max-w-full`.

## [2026-09-08] Preferensi Desain Resmi User: Signature Aesthetic Landing Page
- Apa yang disukai: User sangat menyukai gaya visual landing page Droppr (Stitch/Awwwards style) dan menginstruksikan agar gaya ini dijadikan standar identitas visual utama untuk halaman publik & otentikasi (Landing, Login, Register).
- Karakteristik desain yang disukai:
  1. Kanvas gelap pekat Midnight Obsidian (`#07090E` / `bg-bg-base`).
  2. Background dot matrix interaktif (`HeroDotGrid`) yang merespons kursor mouse pada 60 FPS dengan spotlight radial pendaran amber (`#F0A93B`) & violet (`#8B7FE8`).
  3. Gelombang neon aurora ribbon (`AuroraWave`) yang melengkung mulus dari kiri bawah ke kanan atas tanpa garis batas yang terpotong.
  4. Komponen card frosted glass transparan: `bg-[#0f1420]/50 backdrop-blur-2xl border border-white/15 shadow-[0_20px_50px_-10px_rgba(0,0,0,0.6),inset_0_1px_1px_0_rgba(255,255,255,0.2),0_0_35px_-5px_rgba(139,127,232,0.25)]` dengan rounded corners modern (`rounded-2xl`).
  5. Form input semi-transparan `bg-white/[0.04]` dengan `border-white/15` dan fokus ring amber.
  6. Tipografi Plus Jakarta Sans modern dengan tracking ketat dan aksen badge mono fungsional.
- Aturan ke depan: Untuk setiap penambahan atau pembaruan UI pada halaman publik, auth, atau marketing, selalu gunakan signature aesthetic landing page ini secara konsisten.

## [2026-09-08] SVG default overflow:hidden memotong pendaran blur gradient dan HTML entities Telegram tidak terdecode
- Apa yang salah: Gradient violet di pojok kiri bawah landing page terpotong garis horizontal tajam, dan teks airdrop yang disinkronkan dari Telegram memunculkan string raw HTML entity seperti `&#036;` (bukan `$`).
- Kenapa terjadi (root cause, bukan cuma gejala): Elemen `<svg>` secara spesifikasi W3C memiliki property bawaan `overflow: hidden`. Komponen pita cahaya aurora membungkus filter feGaussianBlur di dalam SVG fixed height, sehingga setiap blur stroke yang meluber ke bawah terpotong rata. Selain itu, scraper Telegram web mengembalikan teks mentah berformat HTML entity yang belum dikonversi ke karakter standar.
- Perbaikan yang dilakukan: Mengatur container SVG aurora wave menjadi `inset-0 w-full h-full` dengan class `overflow-visible`, menambahkan ambient bleed glow di pojok bawah untuk memastikan transisi warna mulus tanpa garis potong, serta menambahkan regex decoder entity HTML (`&#036;`, `&amp;`, `&quot;`, dll.) pada modul scraper Telegram `app/api/feed/live/route.ts`.
- Aturan ke depan: Pastikan elemen SVG yang menggunakan efek gaussian blur diberi class `overflow-visible` atau padding/bleed yang cukup agar tidak terpotong garis batas canvas, dan selalu decode HTML entities pada teks hasil web scrape Telegram.


